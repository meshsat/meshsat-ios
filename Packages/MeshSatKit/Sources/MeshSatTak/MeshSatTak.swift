// MeshSatTak: mirrors tak/ in MeshSat Android (Cursor-on-Target XML, the TAK protobuf, the Hub
// TAK feed) minus the ATAK broadcast intent, which has no iOS equivalent.

public enum MeshSatTak {
    public static let module = "MeshSatTak"
}
